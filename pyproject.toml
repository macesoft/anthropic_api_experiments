[project]
name = "working-with-anthropic-api"
version = "0.1.0"
description = "Es mi primer proyecto con Python, para experimentar con la API de Claude"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "anthropic>=0.116.0",
    "python-dotenv>=1.2.2",
]
